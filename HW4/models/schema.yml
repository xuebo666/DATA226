version: 2

models:
  - name: session_summary
    description: "Analytics summary of user sessions"
    columns:
      - name: sessionId
        description: "Unique identifier for each session"
        data_tests:
          - not_null
          - unique
